#include <stdio.h>

void main(){
	//Write a C program to input the length and width of a rectangle and find its perimeter.
	int l=13,b=25;
	int p=2*(l+b);
	printf("Perimeter of length(%d) and breadth(%d) is: %d",l,b,p);
}
